box: node
# Build definition
build:
  # The steps that will be executed on build
    steps:
      # install npm2
      - script:
            name: install npm@2
            code: sudo npm i -g npm
    
      - npm-install
        
      # build the docs    
      - hgen/gulp:
            tasks: build
