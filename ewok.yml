box: node
# Build definition
build:
  # The steps that will be executed on build
    steps:
      - hgen/gulp:
            tasks: build
    after-steps:
      - wantedly/pretty-slack-notify:
            webhook_url: $SLACK_WEBHOOK_URL
